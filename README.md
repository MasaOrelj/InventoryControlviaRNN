# InventoryControlviaRNN
We are pricing electricity market derivatives using LSM method. We are testing two regression approximations, one using the preselected set of basis functions and one using the randomized neural network, where only the optimization in the last layer is needed. We are pricing swing options and BESS agreements.
